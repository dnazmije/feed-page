# feed-page

### Setup

1. On `api` directory:
 - run the command `npm install`
 - run `npm start` to start the json server on `localhost:5000`

2. On root directory: 
- run `npm install`.
- run `mpn start` to start the pojects on `localhost:4200`

